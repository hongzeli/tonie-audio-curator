[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
markers = ["e2e: tests requiring local FFmpeg execution"]

[tool.ruff]
target-version = "py311"
line-length = 120
exclude = [".venv", "workspace", "output"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]
